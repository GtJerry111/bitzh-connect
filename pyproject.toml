[project]
name = "hitsz-connect-verge"
version = "0.1.0"
description = "A GUI for connecting to the HITSZ Verge network."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "nuitka>=4.0.8",
    "packaging>=26.2",
    "pyobjc>=12.1 ; sys_platform == 'darwin'",
    "pyside6>=6.11.0",
    "requests>=2.33.1",
]
